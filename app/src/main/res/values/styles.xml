<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Avatar">
        <item name="android:src">@android:drawable/sym_def_app_icon</item>
    </style>

    <style name="Avatar.Small">
        <item name="android:layout_width">@dimen/avatar_small_width</item>
        <item name="android:layout_height">@dimen/avatar_small_height</item>
    </style>

    <style name="Avatar.Large">
        <item name="android:layout_width">@dimen/avatar_large_width</item>
        <item name="android:layout_height">@dimen/avatar_large_height</item>
    </style>

</resources>
